apiVersion: v1
kind: Service
metadata:
  name: voting-service
  labels:
    name:  voting-service
    app: demo-voting-app
spec:
  type: NodePort
  ports:
  - name: vote
    port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30033
  selector:
    name: vote
    app: demo-voting-app
